---
import Input from "./Input.astro";
---

<!-- CONTACTO -->
<section id="contacto" class="relative mt-[200px] flex gap-[40px] max-md:flex-col">
    <!-- BLUR -->
     <div class="w-[300px] h-[300px] bg-purple-600 rounded-full absolute bottom-[-100px] left-[-100px] -z-10 blur-[120px] opacity-50"></div>
    <!-- PICTURE -->
     <img 
            class="absolute top-[-10%] right-[-5%] anm max-sm:w-[100px]"
            src="/estrella.avif" 
            alt="estrella">
    <!-- DESCRIPTION -->
     <div class="w-[50%] max-md:w-full">
        <h3 class="block mb-[30px] italic">Contacto</h3>
        <p class="block mb-[20px]">Si tienes consultas, sugerencias o simplemente deseas establecer una conexión más cercana con nuestro equipo, estás en el lugar correcto. </p>
        <p class="block mb-[20px]">Para enviarnos mensajes que incluyan videos, imágenes, currículums u otros archivos, te recomendamos utilizar nuestro correo electrónico <span class="italic font-semibold text-blue-600">soporte@kaoticode.com</span></p>
        <p class="block mb-[20px]">Estamos comprometidos a revisar detalladamente cualquier información que nos envíes.</p>
        
     </div>
    <!-- FORM -->
    <form 
        class=" w-[50%] max-md:w-full"
        action=""
        method="">
        <Input 
            title="Nombre :"
            type="text"
        />
        <div class="flex gap-[10px]">
            <Input 
                title="Correo electrónico :"
                type="email"
            />
            <Input 
                title="Teléfono :"
                type="number"
            />
        </div>
        <label class="block mb-[20px]">
            <p class="block mb-[5px]">Mensaje</p>
            <textarea class="p-[13px]  border-[1px] border-white/30 rounded-[6px] w-full placeholder:text-white outline-none text-[17px] bg-white/0"></textarea>
        </label>
        <input 
            class="font_secundary italic w-full p-[14px] bg-blue-600 rounded-[6px] text-[18px] cursor-pointer hover:bg-blue-700 duration-300"
            type="submit" 
            value="Enviar">
    </form>
</section>

<style>
    .anm{
        animation: anm 8s infinite ;
    }
    @keyframes anm {
        0%,
        100%{
            transform: translateY(-10px);
        }
        50%{
            transform: translateY(20px);
        } 
    }
</style>